#include "main.h"
#include<string.h>
/**
  * _strlen - return lenth of a string
  * @s: points to an character
  * strlen - to return lenth of string
  *
  * Return: strlen
  */

int _strlen(char *s)
{
	return (strlen(s));
}
